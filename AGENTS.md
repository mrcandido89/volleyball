# AGENTS.md

## Cursor Cloud specific instructions

This is an empty repository (`volleyball`). There are currently no source files, dependencies, build systems, or services.

- **No package manager or dependencies** are configured yet. When code is added, update the update script via `SetupVmEnvironment` to match the chosen stack (e.g., `npm install`, `pip install -r requirements.txt`).
- **No lint, test, or build commands** exist. These will need to be configured once source code is added.
- **No services to start.** There is no dev server, database, or other infrastructure.
- The repository is on the `main` branch with a single initial commit.
